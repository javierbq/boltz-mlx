# Optional bundled fixtures

No production checkpoint is committed to the repository. For a self-contained internal build, copy a small exported model folder and precomputed feature folder here, add them to `project.yml`, and load them through `Bundle` in the view model.
